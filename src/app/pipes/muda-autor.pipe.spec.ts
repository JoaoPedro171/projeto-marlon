import { MudaAutorPipe } from './muda-autor.pipe';

describe('MudaAutorPipe', () => {
  it('create an instance', () => {
    const pipe = new MudaAutorPipe();
    expect(pipe).toBeTruthy();
  });
});
